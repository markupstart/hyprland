
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#
# Slow app launch fix
exec-once = systemctl --user import-environment &
exec-once = hash dbus-update-activation-environment 2>/dev/null &
exec-once = dbus-update-activation-environment --systemd &
#exec-once = xdg-desktop-portal-hyprland &

env = GDK_SCALE,1
#env = XCURSOR_SIZE,48
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
#env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1
#env = QT_QPA_PLATFORM,wayland

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=,preferred,auto,auto
monitor=HDMI-A-1,2560x1440@120, 0x0, 1
#monitor=HDMI-A-1,4096x2160@120, 0x0, 1
#monitor=HDMI-A-1,4096x2160, 0x0, 1

#$w1 = hyprctl hyprpaper wallpaper "HDMI-A-1,~/wall.jpg" 
#$w2 = hyprctl hyprpaper wallpaper "HDMI-A-1,contain:~/times.jpg" 
#$w3 = hyprctl hyprpaper wallpaper "HDMI-A-1,contain:~/stat.jpg" 
#$w4 = hyprctl hyprpaper wallpaper "HDMI-A-1,contain:~/build.jpg" 

workspace = 1, name: 🌐, monitor:HDMI-A-1
workspace = 2, name: 📁, monitor:HDMI-A-1
workspace = 3, name: 👾, monitor:HDMI-A-1
workspace = 4, name: 💬, monitor:HDMI-A-1
workspace = 5, name: 📼, monitor:HDMI-A-1
workspace = 6, name: 📷, monitor:HDMI-A-1
workspace = 7, name: 📸, monitor:HDMI-A-1
workspace = 8, name: 📜, monitor:HDMI-A-1
workspace = 9,  name: 🪶, monitor:HDMI-A-1
workspace = 10, name: 😉, monitor:HDMI-A-1
"on-click": "activate",
"on-scroll-up": "hyprctl dispatch workspace e+1",
"on-scroll-down": "hyprctl dispatch workspace e-1"

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
#Eww bar
  exec-once = swayidle -w timeout 300 'swaylock -f -c 000000' before-sleep 'swaylock -f -c 000000'
  exec-once = hyprpaper & waybar
  exec-once = lxpolkit &
  exec-once = gamemoded &
  exec-once = dunst &
  exec-once = copyq --start-server

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =
F
 #   touch {
  #      natural_scroll = no
   # }

    #sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 0
    gaps_out = 0
    border_size = 7
    col.active_border = rgba(a86432ee) rgba(a86432ee) 90deg   
    col.inactive_border = rgba(595959aa)

    layout = master
}
master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    special_scale_factor = 0.8
    mfact = 0.70
    new_is_master = false
    new_on_top = false
    no_gaps_when_only = false
    orientation = right
    inherit_fullscreen = true
    always_center_master = false
}
decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

   rounding =15
   #blur = yes
   #blur_size = 10
   #blur_passes = 1
   #blur_new_optimizations = on

   drop_shadow = yes
   shadow_range = 15
   shadow_render_power = 3
   col.shadow = rgba(595959aa)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = wind, 0.05, 0.9, 0.1, 1.05
    bezier = winIn, 0.1, 1.1, 0.1, 1.1
    bezier = winOut, 0.3, -0.3, 0, 1
    bezier = liner, 1, 1, 1, 1

    animation = windows, 1, 6, wind, slide
    animation = windowsIn, 1, 6, winIn, slide
    animation = windowsOut, 1, 5, winOut, slide
    animation = windowsMove, 1, 5, wind, slide
    animation = border, 1, 10, default
    animation = borderangle, 1, 30, liner, loop
    animation = fade, 1, 7, default
    animation = workspaces, 1, 7, wind
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
#    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
#    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
#    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
#    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
#device:epic-mouse-v1 {
#    sensitivity = -0.5
}

# Example windowrule v1
windowrule = float, ^(qalculate-gtk)$
# Example windowrule v2
#windowrulev2 = float,class:^(qalculate)$,title:^(qalculate)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exit,
bind = $mainMod, D, exec, discord
bind = $mainMod, A, exec, "alacritty -t fzf-nova --dimensions 90 20 --position 350 200 -e /home/mark/fzf_nova/fzf-nova"
bind = $mainMod, G, exec, lutris
bind = $mainMod, A, exec, alacritty
bind = $mainMod, F, exec, firefox
bind = $mainMod, O, exec, obs
bind = $mainMod, C, killactive, 
bind = $mainMod, R, exec, 
bind = $mainMod, T, exec, thunar
bind = $mainMod, SPACE, togglefloating, 
#bind = $mainMod, P, exec, rofi -show combi
bind = $mainMod, P, exec, fuzzel
bind = $mainMod, S, pseudo, # dwindle
#bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind=  $mainMod, 1, exec, $w1 
bind = $mainMod, 2, workspace, 2
bind=  $mainMod, 2, exec, $w2 
bind = $mainMod, 3, workspace, 3
bind=  $mainMod, 3, exec, $w3 
bind = $mainMod, 4, workspace, 4
bind=  $mainMod, 4, exec, $w4
bind = $mainMod, 5, workspace, 5
bind=  $mainMod, 5, exec, $w3
bind = $mainMod, 6, workspace, 6
bind=  $mainMod, 6, exec, $w2 
bind = $mainMod, 7, workspace, 7
bind=  $mainMod, 7, exec, $w1 
bind = $mainMod, 8, workspace, 8
bind=  $mainMod, 8, exec, $w3 
bind = $mainMod, 9, workspace, 9
bind=  $mainMod, 9, exec, $w4 
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
#bind = $mainMod SHIFT, 1, exec,$w1         
bind = $mainMod SHIFT, 2, movetoworkspace, 2
#bind = $mainMod SHIFT, 2, exec,$w2         
bind = $mainMod SHIFT, 3, movetoworkspace, 3
#bind = $mainMod SHIFT, 3, exec,$w3         
bind = $mainMod SHIFT, 4, movetoworkspace, 4
#bind = $mainMod SHIFT, 4, exec,$w4         
bind = $mainMod SHIFT, 5, movetoworkspace, 5
#bind = $mainMod SHIFT, 5, exec,$w3         
bind = $mainMod SHIFT, 6, movetoworkspace, 6
#bind = $mainMod SHIFT, 6, exec,$w2         
bind = $mainMod SHIFT, 7, movetoworkspace, 7
#bind = $mainMod SHIFT, 7, exec,$w1         
bind = $mainMod SHIFT, 8, movetoworkspace, 8
#bind = $mainMod SHIFT, 8, exec,$w3         
bind = $mainMod SHIFT, 9, movetoworkspace, 9
#bind = $mainMod SHIFT, 9, exec,$w4         
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/ windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Move/ windows with mainMod + Keyboard
bind = SUPER, K, layoutmsg, swapprev
bind = SUPER, J, layoutmsg, swapnext
bind = SUPER, Return, layoutmsg, swapwithmaster

bind = SUPER,F10,pass,^(com\.obsproject\.Studio)$

movewindow

bind = $mainMod, L, resizeactive, 110 0
bind = $mainMod, H, resizeactive, -110 0

bind = SUPER, Z, togglespecialworkspace, spbtop # Show spbtop scratchpad
bind = SUPER SHIFT, Z, movetoworkspace, special:spbtop # Move Window to scratchpad spbtop (in case of killing btop)

bind=SUPER SHIFT,F,fullscreen 

bind = SUPER, X, togglespecialworkspace, spbtop2 # Show spbtop2 scratchpad
bind = SUPER SHIFT, X, movetoworkspace, special:spbtop2 # Move Window to scratchpad spbtop2 (in case of killing btop)

bind = SUPER, Y, layoutmsg, orientationleft # tile layout with master on left
bind = SUPER, I, layoutmsg, orientationright # tile layout with master on right
bind = SUPER, U, layoutmsg, orientationcenter # centered master layout

bind = SUPER, M, layoutmsg, addmaster # add focused window to master stack
bind = SUPER SHIFT, M, layoutmsg, removemaster # add Remove window from master stack

# Example volume button that allows press and hold, volume limited to 150%
binde=, XF86AudioRaiseVolume, exec, pw-volume change +2.5%; pkill -RTMIN+8 waybar

# Example volume button that will activate even while an input inhibitor is active
bindl=, XF86AudioLowerVolume, exec, pw-volume change -2.5%; pkill -RTMIN+8 waybar

bindl=, XF86AudioMute, exec, pw-volume mute toggle; pkill -RTMIN+8 waybar

bind=,Print,exec,grim







